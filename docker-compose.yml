services:
  frontend-cadastroclientesddc:
    build:
      context: .
      dockerfile: Dockerfile
    pull_policy: build
    restart: always
    ports:
      - "3001:3001"
    networks:
      - pi_default
networks:
  pi_default:
    external: true